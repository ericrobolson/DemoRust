A simple project used for generating C# code as I find it boiler plate ridden.

# Roadmap

- [ ] Functions
- [ ] Classes

If i was gonna do a lisp, need:

- setq
- lambda

Example fibbonaci:

```
(setq hello (lambda arg)
	(if (eq? arg 1)
		"HI"
	)
)

```
