[package]
name = "server"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
core_connection = {path="../core_connection"}
core_game = {path="../core_game", features=["serde_support"]}
